# Solo para deploy con el backend en otro origen (ahí sí hace falta CORS en Spring).
# En desarrollo dejarlo sin definir: client.ts usa rutas relativas y responde el proxy de Vite.
# VITE_API_BASE=https://api.midominio.com
